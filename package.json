{
  "name": "1c-query-constructor",
  "version": "0.0.0",
  "private": true,
  "description": "Конструктор запросов 1С для VS Code",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
